# pnpm config — applies to frontend/ and dashboard/
# Hoisted layout required for React Native / Metro bundler compatibility.
# Disk savings still apply: packages are hardlinked from pnpm's global store.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
