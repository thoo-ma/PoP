# pnpm config (repo-wide)
# Isolated linker with public hoisting gives a flat node_modules (required for
# React Native / Metro).
node-linker=isolated
auto-install-peers=true
strict-peer-dependencies=false
public-hoist-pattern[]=*
