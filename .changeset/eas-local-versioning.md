---
"pop": patch
---

Switch EAS to local versioning (appVersionSource: local), sync app.json version to 0.2.0, add buildNumber/versionCode fields, update version script to reset build numbers on version bump, and publish OTA updates to both production and preview channels on push to main.
