---
"pop": patch
---

Define semantic token scale (typography, spacing, border-radius, border-width) in global.css and update all tv() style recipes to use token-backed class names instead of arbitrary bracket values.
