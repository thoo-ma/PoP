---
"pop": patch
---

Fix iOS deployment target (15.0 → 15.1) to satisfy ExpoModulesCore minimum requirement; pin EAS iOS build image to `latest` (Xcode 26.2 / Swift 6.2.4) to resolve Swift compiler/SDK version mismatch.
