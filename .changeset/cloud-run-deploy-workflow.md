---
"cloud-run": patch
---

Add Cloud Run deploy workflow triggered by `cloud-run@*` tags and manual `workflow_dispatch`. Add `google-cloud-run/package.json` Changesets shim and register the package in `pnpm-workspace.yaml`.
