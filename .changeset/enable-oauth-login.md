---
"pop": minor
---

Enable Google, X, and Apple OAuth sign-in for alpha release. Fix exchangeCodeForSession bug (parse auth code from callback URL). Add native Apple Sign-In via expo-apple-authentication (iOS only). Remove dead OAuth dialog.
